def verificar_acceso(edad):
    if edad >= 18:
        return "Acceso permitido"
    else:
        return "Acceso denegado" 
edad_usuario = int(input("Ingrese su edad: "))
print(verificar_acceso(edad_usuario))